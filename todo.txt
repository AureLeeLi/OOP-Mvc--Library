# composer install et libraries update

# server php -S 127.0.0.1:8000 -t public

# modif le database : nom de la db et namespace a redéfinir

# modif le namespace de la View et voir pour la méthode de redirection ($url)

# namespace M2i\BookMvc\ ?

# controller 
    -> BookController voir UserController
        -> méthodes à definir
         all (class Book methode list)
         show
         create
         update
         delete

#=> Model -> Book extends Model
    propriétés id, title, price, discount, isbn, author, published_at, image


#views
partials header et footer, + :

    ->allBooks.html.php [route : ['GET', '/livres', 'BookController@list'],]
    localhost:8000/livres

    ->showBook.html.php (livre cliqué) : ['GET', '/livres/[i:id]', 'BookController@show'],//recherche par id -> i = integer;
    ex : localhost:8000/livre/1

    ->createBook.html.php (insertion de livre) : ['GET|POST', '/book/creer', 'BookController@create'],

    ->deleteBook.html.php (suppression de livre) : 
    ['GET', '/book/[i:id]/delete', 'BookController@delete'],
    ex : localhost:8000/book/1/delete

    ->editBook.html.php (update sql) : ['GET', '/book/[i:id]/edit', 'BookController@edit'],
    ex : localhost:8000/book/1/edit

    ->404.html.php idem
