# composer install et libraries update

# server php -S 127.0.0.1:8000 -t public

# modif le database : nom de la db

# sur la View : voir pour la méthode de redirection ($url)

# controller 
    -> BookController voir (UserController)


#model
-> méthodes à definir sur le Model , sur le Model Book extends Model
    all (class Book methode list)
    show
    create
    update
    delete

#=> Model Book extends Model
    propriétés id, title, price, discount, isbn, author, published_at, image


#views (maquettes de l'ancien tp)
partials header et footer, + :

    ->books.html.php [route : ['GET', '/books', 'BookController@list'],]
    localhost:8000/books

    ->book.html.php (livre cliqué) : ['GET', '/book/[i:id]', 'BookController@show'],//recherche par id -> i = integer;
    ex : localhost:8000/book/1

    ->create.html.php (insertion de livre) : ['GET|POST', '/book/creer', 'BookController@create'],

    ->delete.html.php (suppression de livre) : 
    ['GET', '/book/[i:id]/delete', 'BookController@delete'],
    ex : localhost:8000/book/1/delete

    ->editBook.html.php (update sql) : ['GET', '/book/[i:id]/edit', 'BookController@edit'],
    ex : localhost:8000/book/1/edit

    ->404.html.php idem
